name: One-shot request management patcher

# Temporary migration workflow. Delete immediately after the patch lands.
on:
  push:
    branches:
      - feature/build-network
    paths:
      - '.github/workflows/request-management-patcher.yml'

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/build-network
      - name: Add decline and cancel request lifecycle
        run: |
          python3 - <<'PY'
          from pathlib import Path

          engine_path = Path('bin/omarchy-friends')
          engine = engine_path.read_text(encoding='utf-8')

          def replace_once(text, old, new, label):
              if old not in text:
                  raise SystemExit(f'missing patch anchor: {label}')
              return text.replace(old, new, 1)

          engine = replace_once(
              engine,
              '    "friend_accept": ("✅", "a friend acceptance"),\n    "coffee":',
              '    "friend_accept": ("✅", "a friend acceptance"),\n    "friend_decline": ("✕", "a declined friend request"),\n    "friend_cancel": ("↩", "a cancelled friend request"),\n    "coffee":',
              'request action metadata',
          )

          accept_tail = '''        self._mark_friends_memory(ping["public_key"], ping.get("handle", ""), ping.get("avatar", ""))
        self.save_state()
        return True, "You are now friends"

    def _supports_nip17(self, public_key):
'''
          request_methods = '''        self._mark_friends_memory(ping["public_key"], ping.get("handle", ""), ping.get("avatar", ""))
        self.save_state()
        return True, "You are now friends"

    def decline_friend_request(self, ping_id):
        """Decline one incoming request and notify the original sender."""
        ping_id = trim_text(ping_id, 64).lower()
        pings = self.state.setdefault("global", {}).setdefault("pings", [])
        ping = next((item for item in pings if item.get("id") == ping_id), None)
        if not ping or ping.get("action") != "friend_request":
            return False, "That friend request is no longer available"
        ok, message = self.global_ping(
            ping["public_key"],
            "friend_decline",
            session_id=ping.get("session_id"),
            allow_offline=True,
        )
        if not ok:
            return False, message
        self.state["global"]["pings"] = [item for item in pings if item.get("id") != ping_id]
        self.save_state()
        return True, "Friend request declined"

    def cancel_friend_request(self, public_key):
        """Cancel one outgoing pending request and notify the recipient."""
        public_key = trim_text(public_key, 64).lower()
        friendships = self.state.setdefault("global", {}).setdefault("friendships", {})
        friendship = friendships.get(public_key, {})
        if friendship.get("status") != "pending":
            return False, "That sent request is no longer pending"
        ok, message = self.global_ping(
            public_key,
            "friend_cancel",
            session_id=friendship.get("request_id"),
            allow_offline=True,
        )
        if not ok:
            return False, message
        friendships.pop(public_key, None)
        self.save_state()
        return True, "Friend request cancelled"

    def _supports_nip17(self, public_key):
'''
          engine = replace_once(engine, accept_tail, request_methods, 'request lifecycle methods')

          ingest_anchor = '        if ping.get("action") == "friend_accept":\n'
          ingest_handlers = '''        if ping.get("action") == "friend_decline":
            friendship = global_state.setdefault("friendships", {}).get(ping["public_key"], {})
            if friendship.get("status") == "pending" and (
                not ping.get("session_id")
                or not friendship.get("request_id")
                or friendship.get("request_id") == ping.get("session_id")
            ):
                global_state["friendships"].pop(ping["public_key"], None)
                self._remember_memory_signal(ping["public_key"], "friend_decline", "received", ping.get("handle", ""), ping.get("avatar", ""))
                self._append_event(
                    "friend_decline", "✕", ping.get("handle"), ping.get("avatar"),
                    f"{ping.get('handle', 'A builder')} declined your friend request.", ping["id"]
                )
            processed_ids.append(ping["id"])
            global_state["processed_event_ids"] = processed_ids[-MAX_SEEN_EVENT_IDS:]
            return True
        if ping.get("action") == "friend_cancel":
            session_id = ping.get("session_id")
            global_state["pings"] = [
                item for item in global_state.get("pings", [])
                if not (
                    item.get("action") == "friend_request"
                    and item.get("public_key") == ping["public_key"]
                    and (
                        not session_id
                        or not item.get("session_id")
                        or item.get("session_id") == session_id
                    )
                )
            ]
            self._remember_memory_signal(ping["public_key"], "friend_cancel", "received", ping.get("handle", ""), ping.get("avatar", ""))
            processed_ids.append(ping["id"])
            global_state["processed_event_ids"] = processed_ids[-MAX_SEEN_EVENT_IDS:]
            return True
        if ping.get("action") == "friend_accept":
'''
          engine = replace_once(engine, ingest_anchor, ingest_handlers, 'request control ingest')

          cli_anchor = '''    elif command == "accept-friend" and len(sys.argv) >= 3:
        ok, message = engine.accept_friend_request(sys.argv[2])
        print_result(ok, message)
'''
          cli_replacement = '''    elif command == "decline-friend" and len(sys.argv) >= 3:
        ok, message = engine.decline_friend_request(sys.argv[2])
        print_result(ok, message)
    elif command == "cancel-friend" and len(sys.argv) >= 3:
        ok, message = engine.cancel_friend_request(sys.argv[2])
        print_result(ok, message)
    elif command == "accept-friend" and len(sys.argv) >= 3:
        ok, message = engine.accept_friend_request(sys.argv[2])
        print_result(ok, message)
'''
          engine = replace_once(engine, cli_anchor, cli_replacement, 'request CLI')
          engine_path.write_text(engine, encoding='utf-8')

          panel_path = Path('FriendsPanelV3.qml')
          panel = panel_path.read_text(encoding='utf-8')

          function_anchor = '    function toggleGroupMember(publicKey) {\n'
          function_replacement = '''    function declineFriendRequest(pingId) {
        if (!root.service || !pingId) return
        root.service.runAction([root.service.binPath, "decline-friend", pingId], function(output) {
            root.service.reportResult(output, "Friend request declined")
            root.service.refresh()
        })
    }

    function cancelFriendRequest(publicKey) {
        if (!root.service || !publicKey) return
        root.service.runAction([root.service.binPath, "cancel-friend", publicKey], function(output) {
            root.service.reportResult(output, "Friend request cancelled")
            root.service.refresh()
        })
    }

    function toggleGroupMember(publicKey) {
'''
          panel = replace_once(panel, function_anchor, function_replacement, 'request UI helpers')

          panel = replace_once(
              panel,
              'width: parent.width - acceptRequestButton.width - Style.space(62)',
              'width: parent.width - acceptRequestButton.width - declineRequestButton.width - Style.space(70)',
              'received request column width',
          )
          accept_anchor = '''                                                GlassButton {
                                                    id: acceptRequestButton
                                                    text: "Accept"
'''
          accept_replacement = '''                                                GlassButton {
                                                    id: declineRequestButton
                                                    text: "Decline"
                                                    compact: true
                                                    anchors.verticalCenter: parent.verticalCenter
                                                    onClicked: root.declineFriendRequest(modelData.id)
                                                }
                                                GlassButton {
                                                    id: acceptRequestButton
                                                    text: "Accept"
'''
          panel = replace_once(panel, accept_anchor, accept_replacement, 'received decline button')

          sent_pending = '''                                                GlassPill { anchors.verticalCenter: parent.verticalCenter; text: "Pending"; active: true; accentColor: root.warning }
'''
          sent_actions = '''                                                Row {
                                                    anchors.verticalCenter: parent.verticalCenter
                                                    spacing: Style.space(6)
                                                    GlassPill { text: "Pending"; active: true; accentColor: root.warning }
                                                    GlassButton { text: "Cancel"; compact: true; onClicked: root.cancelFriendRequest(modelData.public_key) }
                                                }
'''
          panel = replace_once(panel, sent_pending, sent_actions, 'sent cancel button')
          panel_path.write_text(panel, encoding='utf-8')
          PY

          python3 -m py_compile bin/omarchy-friends
          git diff --check
          git config user.name "omarchy-friends-migration"
          git config user.email "67705498+harshithnadig@users.noreply.github.com"
          git add bin/omarchy-friends FriendsPanelV3.qml
          git commit -m "feat: add decline and cancel friend requests"
          git push origin HEAD:feature/build-network
